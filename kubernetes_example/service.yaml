apiVersion: v1
kind: Service
metadata:
  name: torch-service
spec:
  selector:
    app: torch
  ports:
    - protocol: TCP
      port: 8080        # Service port inside cluster
      targetPort: 8080  # Container port (adjust if your app listens elsewhere)
      nodePort: 30080   # External port on each node
  type: NodePort
